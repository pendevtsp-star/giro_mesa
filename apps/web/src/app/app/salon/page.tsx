"use client";
import { LayoutGrid, Save, Users } from "lucide-react";
import { type DragEvent, useEffect, useState } from "react";
import { getFloorPlan, getSession, listTables, saveFloorPlan, type DiningTable } from "../../../lib/giromesa-api";
const tones: Record<string, string> = { free: "free", occupied: "occupied", preparing: "preparing", waiting_payment: "payment", reserved: "reserved", served: "served", order_sent: "preparing", waiting_order: "reserved" };
type Position = { x: number; y: number };
export default function SalonPage() { const [tables, setTables] = useState<DiningTable[]>([]); const [branchId, setBranchId] = useState(""); const [layout, setLayout] = useState<Record<string, Position>>({}); const [message, setMessage] = useState("Carregando mapa do salão..."); const [dragged, setDragged] = useState<string | null>(null); useEffect(() => { void (async () => { try { const session = await getSession(); if (!session.branchId) throw new Error(); setBranchId(session.branchId); const [rows, plan] = await Promise.all([listTables(session.branchId), getFloorPlan(session.branchId)]); setTables(rows); setLayout(plan.layout); setMessage("Arraste as mesas para organizar o salão e salve a disposição."); } catch { setMessage("Entre com um perfil operacional para carregar o mapa real."); } })(); }, []); function drop(event: DragEvent<HTMLElement>) { event.preventDefault(); if (!dragged) return; const rect = event.currentTarget.getBoundingClientRect(); setLayout((current) => ({ ...current, [dragged]: { x: Math.max(0, Math.min(88, ((event.clientX - rect.left) / rect.width) * 100)), y: Math.max(0, Math.min(82, ((event.clientY - rect.top) / rect.height) * 100)) } })); setDragged(null); } async function persist() { if (!branchId) return; try { await saveFloorPlan(branchId, layout); setMessage("Disposição do salão salva para esta unidade."); } catch { setMessage("Não foi possível salvar a disposição."); } } return <main className="salon-page"><header className="kds-topbar"><a className="brand" href="/app"><span className="brand-mark">G</span><span>GiroMesa</span></a><div className="toolbar"><button className="button primary compact" onClick={() => void persist()} type="button"><Save size={16} /> Salvar mapa</button><a className="button secondary compact" href="/app?view=pos">Abrir PDV</a></div></header><section className="salon-header"><span className="section-kicker"><LayoutGrid size={16} /> Salão</span><h1>Mapa de mesas</h1><p>{message}</p></section><section className="salon-legend"><span className="free">Livre</span><span className="occupied">Em atendimento</span><span className="preparing">Em preparo</span><span className="payment">Pagamento</span></section><section className="salon-board salon-positioning-board" onDragOver={(event) => event.preventDefault()} onDrop={drop}>{tables.map((table, index) => { const position = layout[table.id] ?? { x: (index % 4) * 24 + 4, y: Math.floor(index / 4) * 28 + 4 }; return <a draggable className={`salon-table salon-positioned ${tones[table.status] ?? "reserved"}`} href={`/app?view=pos&table=${table.id}`} key={table.id} onDragStart={() => setDragged(table.id)} onClick={(event) => { if (dragged) event.preventDefault(); }} style={{ left: `${position.x}%`, top: `${position.y}%` }}><strong>{table.code}</strong><span>{table.name}</span><small><Users size={14} />{table.seats} lugares</small></a>; })}</section></main>; }
